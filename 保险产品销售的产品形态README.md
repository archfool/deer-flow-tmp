# 保险产品销售对话机器人：产品形态 README

本文档描述我们计划基于 DeerFlow 构建的“保险产品销售对话机器人”的产品形态。它面向业务、产品和方案设计人员，重点说明这个产品要解决什么问题、服务谁、有哪些任务、用户如何使用，以及当前第一阶段已经做到什么程度。

技术实现细节、代码模块位置、工作流运行时、客户档案记忆和 skill 拆解方案，请看同目录下的《保险产品销售的技术模块README.md》。

## 1. 产品定位

这个对话机器人不是一个泛问答机器人，而是一个面向保险代理人的“对话式业务工作台”。

目标用户是保险代理人，也就是负责客户经营、保险销售、方案讲解和售后服务的一线销售人员。机器人协助他们通过自然语言对话完成日常工作任务，例如：

- 分析客户需求；
- 检查客户现有保障是否充分；
- 生成保障缺口诊断；
- 推荐保障配置方向；
- 辅助销售沟通与异议处理；
- 做产品利益、现金流或方案测算。

产品的核心价值不是“回答保险知识问题”，而是把保险销售过程中的复杂任务拆成可执行、可追踪、可复用的业务流程，并通过对话把这些流程串起来。

## 2. 目标用户与使用对象

### 2.1 使用者：保险代理人

保险代理人使用机器人完成自己的销售和客户经营工作。典型需求包括：

- 快速整理某个客户的家庭与财务情况；
- 根据客户档案生成保障检视报告；
- 发现客户保障缺口；
- 准备下一次客户沟通话术；
- 将客户的补充信息更新回客户档案；
- 在多个任务之间自由切换，而不是被固定流程卡住。

### 2.2 被分析对象：代理人的客户

机器人分析的对象是代理人的客户，通常以“家庭”为单位进行建模。

第一阶段我们明确采用：

- 每个 `user_id` 表示一个代理人或系统用户；
- 每个 `customer_id` 表示该代理人名下的一个客户或客户家庭；
- 客户数据按 `user_id + customer_id` 严格隔离；
- 不允许不同代理人之间共享客户档案；
- 后续即使出现同名客户，也不会跨用户合并。

这种隔离方式是产品上的底线要求，因为保险客户信息属于高敏感数据。

## 3. 产品核心形态

### 3.1 对话式业务工作台

用户进入系统后，不需要先选择一个固定表单或固定流程，而是可以直接用自然语言说：

- “帮我看一下张先生家的保障缺口。”
- “先做保障检视，但养老部分先跳过。”
- “刚才客户补充了房贷余额，更新到档案里。”
- “现在先帮我准备一段销售沟通话术，等会再回到保障检视。”
- “把客户版报告和内部诊断版都生成一下。”

机器人负责理解用户意图，并把对话映射到一个或多个可执行任务。

### 3.2 任务卡片 + 对话流

理想前端形态不是单纯聊天窗口，而是“聊天 + 任务工作区”的组合。

建议前端包含以下区域：

- 左侧或顶部：当前客户、当前任务、任务状态；
- 主区域：对话窗口；
- 右侧：客户档案、缺失信息、任务产物、报告预览；
- 底部或侧栏：正在运行、暂停、完成、失败的任务列表；
- 报告区：客户可见版和内部诊断版分开展示。

对话仍然是主要入口，但业务产物要结构化展示，不能只混在聊天记录里。

### 3.3 客户档案作为产品中枢

客户档案是这个产品最重要的业务资产之一。机器人不是每次重新问一遍客户信息，而是持续维护每个客户自己的档案。

客户档案包括：

- 客户基本信息；
- 家庭成员结构；
- 经济支柱与收入占比；
- 收入、支出、负债、资产、预算；
- 社保、医保、商业保单；
- 养老、教育、医疗偏好等规划信息；
- 字段来源、更新时间和可信度。

对话过程中，如果用户明确提供了新信息，机器人应把信息写回档案；如果是模型推断出来的信息，必须先让用户确认，不能直接写入正式档案。

## 4. 计划支持的业务任务

保险产品销售场景最多包含以下五类任务。

| 任务 | 目标 | 当前状态 |
| --- | --- | --- |
| 需求分析 | 明确客户家庭、财务、风险偏好、预算、销售机会 | 计划中 |
| 保障检视 | 对现有保障进行六维缺口分析，并生成报告 | 第一阶段已实现后端 MVP |
| 方案推荐 | 基于缺口、预算和产品库给出配置建议 | 计划中 |
| 销售沟通 | 生成客户沟通话术、异议处理、跟进建议 | 计划中 |
| 利益测算 | 做现金流、收益、保单利益或方案对比测算 | 计划中 |

这些任务不是固定线性流程。用户可以只做其中一个，也可以组合执行多个。

例如：

- 只做“保障检视”；
- 先做“需求分析”，再做“保障检视”；
- 做“保障检视”过程中临时切去“销售沟通”；
- 同时启动“保障检视”和“利益测算”；
- 暂停一个任务，补充客户档案后再回来继续。

## 5. 任务自由跳转的产品体验

我们明确不采用“进入某个子 Agent 后用户就被困在里面”的产品体验。

产品上要支持：

- 随时进入某个任务；
- 随时暂停当前任务；
- 随时继续已暂停任务；
- 随时取消任务；
- 在一个任务等待数据时，切去做另一个任务；
- 一个临时任务完成后，回到原任务继续；
- 多个任务可以围绕同一客户档案并行推进；
- 任务之间可以共享结构化产物，但不能互相污染状态。

典型交互如下：

```text
代理人：帮我给张先生做保障检视。
机器人：可以。当前缺少家庭年收入、月支出、房贷余额、已有保单信息。我先基于已有档案列出待确认项。

代理人：先别问这些，客户等会要电话沟通，帮我准备一段开场话术。
机器人：已暂停保障检视，开始生成销售沟通话术。

代理人：话术可以。现在继续刚才的保障检视，客户说家庭年收入 60 万，月支出 2.5 万，房贷还剩 180 万。
机器人：已更新客户档案，并继续保障检视。
```

这种体验要求底层任务运行时必须具备任务状态管理、暂停恢复、输入请求、任务产物记录和并发隔离能力。

## 6. 当前第一阶段实现范围

第一阶段只实现：

1. 客户档案记忆；
2. 通用任务运行时；
3. 保障检视任务；
4. Mock 外部数据 Adapter；
5. 后端 API 和 Agent Tool 接入；
6. JSON 结果；
7. 客户可见版 Markdown 报告；
8. 内部诊断版 Markdown 报告。

前端只做必要适配，不作为第一阶段重点。

数据库策略：

- 开发环境使用 SQLite；
- 生产环境面向 PostgreSQL；
- 使用现有 SQLAlchemy 和 Alembic 迁移体系；
- 不引入新的数据库框架。

外部接口策略：

- 第一阶段先使用 Adapter + Mock；
- 具体外部数据接口由后续系统集成提供；
- 业务代码通过 Adapter 访问数据，不直接依赖具体 HTTP 实现。

## 7. 保障检视的产品定义

保障检视是第一阶段优先实现的核心任务。

它的目标是：围绕一个客户家庭，分析现有保障与目标保障之间的缺口，并生成两类报告：

- 客户可见版：可以给客户展示，语言保守、解释清晰；
- 内部诊断版：给代理人使用，包含缺失信息、假设、置信度、话术建议和风险提示。

### 7.1 家庭任务 + 成员级分析

保障检视按“家庭任务 + 成员级分析”的方式设计。

原因是保险销售通常不是只看一个人，而是要看家庭整体风险：

- 谁是经济支柱；
- 收入由谁贡献；
- 谁承担负债；
- 子女教育责任；
- 配偶和父母赡养责任；
- 家庭现金流压力；
- 已有保单分别覆盖哪些成员。

当前代码已经按家庭档案启动保障检视，并在计算时使用家庭成员、收入占比、保单被保人等信息。完整的逐成员报告和成员级任务分解仍属于后续增强。

### 7.2 六维缺口

保障检视包含六个维度：

1. 寿险；
2. 重疾；
3. 医疗；
4. 意外；
5. 养老；
6. 应急储备。

每个维度都要回答：

- 目标保障是什么；
- 当前保障是什么；
- 缺口在哪里；
- 为什么目标是这个数或这类责任；
- 哪些输入是用户明确提供的；
- 哪些输入来自权威接口；
- 哪些信息缺失；
- 哪些参数只是 DRAFT 参数，不能作为正式行业口径。

### 7.3 三个测算场景

第一阶段输出三个测算场景：

- 保守；
- 基准；
- 充分。

这三个场景用于表达不同保障充足程度下的测算结果，避免只给出一个看似精确但实际上依赖假设的数字。

当前所有未确定绩优参数都进入 DRAFT 参数表，不声明为正式行业标准。

### 7.4 客户事实与测算参数的边界

产品上明确区分“客户事实”和“测算参数”。

客户事实不能使用默认值，例如：

- 年龄；
- 性别；
- 职业；
- 常住城市；
- 家庭成员；
- 收入；
- 支出；
- 负债；
- 保单；
- 保费预算。

如果客户事实缺失，机器人必须标记缺失，并触发追问或等待补充，不能编造。

测算参数可以使用 DRAFT 默认值，例如：

- 重疾治疗费区间；
- 收入损失补偿年数；
- 寿险收入替代年限；
- 养老替代率；
- 应急储备月数。

报告中必须说明这些参数是测算假设。

## 8. 客户档案产品规则

### 8.1 独立档案

每个代理人的每个客户都有独立档案：

```text
user_id + customer_id + memory_type
```

当前客户档案的 `memory_type` 是：

```text
insurance.customer-profile
```

这样可以保证：

- 同一代理人的不同客户互相隔离；
- 不同代理人的客户互相隔离；
- 后续可以为同一客户增加不同类型的记忆文件；
- 客户档案可以被多个任务共享。

### 8.2 可展示、可修正、可追溯

客户档案不是隐藏在模型上下文里的临时记忆，而是一个可读取、可展示、可更新、可审计的业务对象。

用户应该可以随时说：

- “展示一下张先生的客户档案。”
- “把张先生房贷余额改成 180 万。”
- “这个保单不是张先生的，是太太的。”
- “刚才那条健康情况只是猜测，不要写入档案。”

档案更新需要保留来源信息和版本记录，避免不清楚某个字段是怎么来的。

### 8.3 数据来源优先级

第一阶段采用以下策略：

| 来源 | 是否可以直接更新正式档案 | 说明 |
| --- | --- | --- |
| 用户明确提供 | 可以 | 例如代理人在对话里明确说“客户年收入 60 万” |
| 权威接口数据 | 可以 | 例如 CRM、保单系统、客户信息系统返回的数据 |
| LLM 推断 | 不可以直接写入 | 必须生成候选更新，等待用户确认 |
| 系统派生数据 | 当前不作为客户事实直接写入 | 可作为任务结果或分析结果保存 |

这个规则对保险销售尤其重要，因为错误写入客户事实会影响保障测算和销售建议。

## 9. 报告产品形态

保障检视首版输出三类结果：

1. JSON 结构化结果；
2. 客户可见版 Markdown 报告；
3. 内部诊断版 Markdown 报告。

### 9.1 客户可见版

客户可见版报告面向最终客户，语言要克制、解释要清晰，避免夸大风险和承诺收益。

建议包含：

- 抬头或封面；
- 家庭结构摘要；
- 家庭风险概览；
- 六维缺口结果；
- 每个维度的解释、缺口和建议；
- 优先级路线图；
- 预算内取舍建议；
- 下一步行动建议；
- 合规声明与免责。

当前代码已经生成客户可见版 Markdown，但家庭结构图、红黄绿可视化、预算取舍和权益服务衔接还需要后续增强。

### 9.2 内部诊断版

内部诊断版只给代理人使用，不可直接外发给客户。

建议包含：

- 测算假设；
- 参数版本；
- 缺失信息；
- 置信度；
- 业务风险提示；
- 代理人讲解话术；
- 异议预判；
- 系统建议与可执行动作。

当前代码已经把内部诊断版与客户可见版做成完全独立的输出 Schema，避免“同一份报告换个标题”的实现方式。

## 10. 典型用户流程

### 10.1 新建客户档案

```text
代理人：新建一个客户，张先生，38 岁，男，上海，有太太和一个 6 岁孩子。
机器人：已创建客户档案。还缺少收入、支出、负债、社保和已有保单信息。
```

系统动作：

- 创建 `insurance.customer-profile`；
- 写入用户明确提供的事实；
- 记录字段来源；
- 返回缺失项。

### 10.2 启动保障检视

```text
代理人：帮张先生做保障检视。
机器人：可以。当前缺少家庭年收入、月支出、房贷余额、保费预算、已有保单信息。补充后可以生成完整结果。
```

系统动作：

- 创建保障检视任务；
- 读取客户档案；
- 读取 Mock Adapter 数据；
- 检查必需事实；
- 若缺失，返回输入请求；
- 若满足，执行六维分析。

### 10.3 补充事实并继续

```text
代理人：家庭年收入 60 万，月支出 2.5 万，房贷余额 180 万，预算每年 5 万。
机器人：已更新档案，并继续保障检视。
```

系统动作：

- 将用户明确事实写入正式客户档案；
- 任务继续推进；
- 生成结构化结果和两份报告。

### 10.4 临时切换任务

```text
代理人：先暂停这个，帮我准备和客户沟通的开场话术。
机器人：已暂停保障检视。下面是建议开场话术……
```

系统动作：

- 当前保障检视任务进入暂停状态；
- 新建销售沟通任务；
- 销售沟通任务完成后，用户可以恢复保障检视。

## 11. 当前已经实现的产品能力

当前后端已经具备以下能力：

- 按 `user_id + customer_id` 管理客户档案；
- 创建、读取、更新客户档案；
- 区分用户明确数据、权威接口数据和模型推断；
- 模型推断进入候选更新，等待确认；
- 启动保障检视任务；
- 任务可进入等待输入状态；
- 任务可暂停、恢复、取消；
- 保障检视按六个维度计算；
- 输出保守、基准、充分三个场景；
- 生成 JSON 结果；
- 生成客户可见版 Markdown；
- 生成内部诊断版 Markdown；
- 提供 REST API；
- 提供 Agent Tool；
- 使用 SQLAlchemy 持久化；
- 提供 Alembic 数据库迁移；
- 提供单元测试文件。

## 12. 当前尚未完整实现的产品能力

以下能力还没有完整实现：

- 前端完整工作台；
- 任务卡片、任务列表和任务恢复界面；
- 客户档案可视化编辑器；
- 家庭结构图；
- 六维缺口可视化图；
- 完整成员级报告；
- 真实外部数据接口；
- 产品库；
- 方案推荐；
- 产品组合约束；
- 保费预算分配；
- 利益测算；
- 销售沟通任务；
- 合规模板审核；
- 参数治理后台；
- 生产级权限、审计和脱敏能力。

## 13. 产品设计原则

### 13.1 对话自由，不强制线性流程

用户不应该因为进入某个任务而失去自由对话能力。任务应该是可暂停、可恢复、可并行、可嵌套的业务对象。

### 13.2 客户事实必须可信

客户事实不能由模型编造。缺失就是缺失，必须通过用户确认或权威接口补齐。

### 13.3 报告要解释“为什么”

保障检视不是只输出数字。报告必须解释目标保障的计算原因、参数来源和假设边界。

### 13.4 客户版和内部版必须分离

客户版报告面向客户，内部版报告面向代理人。两者内容、语言、风险提示和 Schema 都应该独立。

### 13.5 参数必须可治理

寿险替代年限、重疾治疗费、养老替代率等参数不能硬编码成“行业标准”。未确认参数只能作为 DRAFT 参数，后续需要业务治理。

## 14. 推荐产品路线图

### 阶段 1：保障检视 MVP

当前阶段，目标是把客户档案、任务运行时和保障检视跑通。

交付物：

- 客户档案；
- 通用任务运行时；
- 保障检视；
- JSON 结果；
- 两份 Markdown 报告；
- API 与 Agent Tool。

### 阶段 1.1：保障检视业务化

建议下一步聚焦：

- 完整成员级分析；
- 更严格的必需事实校验；
- 客户版报告美化；
- 内部诊断版话术增强；
- 缺失信息追问策略；
- 参数表治理。

### 阶段 2：需求分析与方案推荐

在客户档案和保障检视稳定后，增加：

- 需求分析任务；
- 客户画像；
- 产品库接入；
- 方案推荐；
- 预算约束；
- 推荐理由解释。

### 阶段 3：销售沟通与利益测算

继续增加：

- 销售沟通任务；
- 异议处理；
- 面谈纪要；
- 利益测算；
- 方案对比；
- 客户跟进计划。

### 阶段 4：生产运营

生产化阶段需要补齐：

- 审计；
- 权限；
- 数据脱敏；
- 合规模板；
- 参数审批；
- 运营看板；
- 质量评估；
- 业务效果追踪。

## 15. 总结

我们计划构建的是一个保险代理人的对话式业务工作台，而不是简单问答机器人。

它的核心产品机制是：

- 用客户档案沉淀客户事实；
- 用任务运行时管理可暂停、可恢复、可并行的业务任务；
- 用 skill 拆解任务步骤；
- 用结构化结果和报告承接业务输出；
- 用确认机制防止模型推断污染客户事实。

第一阶段已经落地“客户档案 + 通用任务运行时 + 保障检视”的后端 MVP。后续应优先把保障检视做深，再扩展需求分析、方案推荐、销售沟通和利益测算。

